
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

// Componente temporal hasta que creemos el nuevo sistema
const DenunciaCard = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Sistema de Denuncias</CardTitle>
      </CardHeader>
      <CardContent>
        <p>El nuevo sistema de denuncias se está configurando...</p>
      </CardContent>
    </Card>
  );
};

export default DenunciaCard;
