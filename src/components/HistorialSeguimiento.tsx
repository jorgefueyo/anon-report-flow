
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

// Componente temporal hasta que creemos el nuevo sistema
const HistorialSeguimiento = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Historial de Seguimiento</CardTitle>
      </CardHeader>
      <CardContent>
        <p>El sistema de seguimiento se está configurando...</p>
      </CardContent>
    </Card>
  );
};

export default HistorialSeguimiento;
